;This is NOT an AI!!!


;Basic Hello World!
;(display "Hello, World!") (newline)


;Set name of Chatbot below 
(define botName "")
;User inputs their name below
(display "Input your name: ") 
(define name (read)) (newline)
;Basic response from the Chatbot
(display name) (display " is a good name!")(newline)
(display "My name is ") (display botName) (display "!")(newline)

